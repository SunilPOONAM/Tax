@model IEnumerable<TaxService.Models.AdminModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}




<link href="~/Content/plugins/DataTables/datatables.css" rel="stylesheet" />
<style>
    table.dataTable tr {
        height: 50px;
    }
</style>




<div class="col-md-12">
    <h2>Admins</h2>
    <p class="text-right">
        <a href="@Url.Action("Create")" class="btn bg-primary">Add New</a>
    </p>

    <div class="table-responsive mt-5">
        <table class="table table-striped table-hover" id="cusTable">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Username)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Role)
                    </th>

                    <th>Action</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Username)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Role)
                        </td>

                        <td class="d-flex">
                            <a href="~/Admin/Details/@item.UserID" data-toggle="tooltip" data-placement="top" title="Details">
                                <i class="fas fa-info-circle"></i>
                            </a>
                            <span class="mx-2">|</span>
                            <a href="~/Admin/Edit/@item.UserID" data-toggle="tooltip" data-placement="top" title="Edit">
                                <i class="fas fa-pencil-alt"></i>
                            </a>
                            <span class="mx-2">|</span>
                            <a href="~/Admin/Delete/@item.UserID" data-toggle="tooltip" data-placement="top" title="Delete">
                                <i class="far fa-trash-alt"></i>
                            </a>

                            <span class="mx-2">|</span>
                            <a href="~/Admin/Change_Password/@item.UserID" data-toggle="tooltip" data-placement="top" title="Change Password">
                                <i class="fas fa-key"></i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>


@section Scripts {


    <script src="~/Content/plugins/DataTables/datatables.min.js"></script>

    <script>
        $(document).ready(function () {
            $('#cusTable').DataTable({

            });
        });
    </script>

}
